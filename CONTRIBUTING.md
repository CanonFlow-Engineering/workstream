# Contributing

Use Node 24.19.0 and run `npm run verify` before proposing a change. Keep the
event ledger append-only. Do not add a remote write path without a separate
human-reviewed design change.
